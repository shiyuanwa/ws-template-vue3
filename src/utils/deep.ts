class Deep {
  public target = []
  constructor(target: []) {
    this.target = target
  }
  private searchIndex() {}

  find(r: string) {
    for (let i = 0; i < this.length; i++) {
      const element = array[i];
      
    }
  }
}

export default Deep
